services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - MODEL_TYPE=xgboost
      - LOG_LEVEL=INFO
      - API_HOST=0.0.0.0
      - API_PORT=8000
    restart: always
